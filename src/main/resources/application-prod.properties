# Production profile for Render deployment
spring.application.name=Journal

# MongoDB with aggressive timeouts for fast startup
spring.data.mongodb.uri=mongodb+srv://ankurkarna:xV4s5CCSJJQEjnQz@cluster0.ih67exj.mongodb.net/journaldb?retryWrites=true&w=majority&appName=Cluster0&connectTimeoutMS=3000&socketTimeoutMS=3000&serverSelectionTimeoutMS=3000&maxPoolSize=3&minPoolSize=1&maxIdleTimeMS=15000&heartbeatFrequencyMS=10000

# Disable auto-index creation completely
spring.data.mongodb.auto-index-creation=false

# Aggressive startup optimizations
spring.main.lazy-initialization=true
spring.main.banner-mode=off
spring.main.web-application-type=servlet

# Minimal server configuration
server.port=8080
server.tomcat.threads.max=10
server.tomcat.threads.min-spare=1
server.tomcat.accept-count=10
server.tomcat.max-connections=100

# Minimal logging
logging.level.root=WARN
logging.level.org.springframework=WARN
logging.level.org.mongodb.driver=ERROR
logging.level.com.karna.ankur.Journal=INFO

# Disable unnecessary features
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Health check endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never 